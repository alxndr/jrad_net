<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :date, class: "control-label" %>
    <%= datetime_select f, :date, class: "form-control" %>
    <%= error_tag f, :date %>
  </div>

  <div class="form-group">
    <%= label f, :venue_id, class: "control-label" %>
    <%
      default_venue_id = cond do
        @changeset.data.venue -> @changeset.data.venue.id
        true -> 0
      end
    %>
    <%= select f, :venue_id,
      [{"...pick venue:", 0} | Enum.into(@all_venues, [], &({"#{&1.location} — #{&1.name}", &1.id}))],
      default: default_venue_id,
      class: "form-control"
    %>
    <%= error_tag f, :venue %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
